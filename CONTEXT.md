# 厦门本岛早高峰共享单车流动挖掘

从 2020-12-21 至 12-25 五个工作日 6:00–10:00 的共享单车 GPS 与订单数据中，划出源汇区域并刻画区域间流动模式。

本文件是术语的**唯一来源**。`docs/project-plan.md` §二 已改为指向此处，不再保留副本。

## 数据单位

**轨迹（track）**：
由单个 `BICYCLE_ID` 的定位点按 `source_row` 顺序切分出的一段独立骑行，标识为 `TRACK_ID`。
_Avoid_: 行程、trip、路径

**行程（trip）**：
订单表中相邻的一对（解锁, 上锁）事件。与轨迹是两个不同的对象，不做逐条配对。
_Avoid_: 订单、order、轨迹

**有效轨迹（valid track）**：
通过**全部九条**硬剔除规则的轨迹，由 `track_match` 表承载。所有区域级统计量只由有效轨迹贡献，且每条一票、不加权。切分阶段的 `tracks.is_valid` 与 `points.is_valid_track` 只表示通过了前六条，不是这个词。
_Avoid_: 干净轨迹、好轨迹

**单元格（cell）**：
EPSG:32650 下边长 150 米、原点对齐到整数倍的正方形网格单元。
_Avoid_: 格子、grid、瓦片

**单元格流网络（cell flow network）**：
有向单元格对及其轨迹数权重构成的网络，是区域发现的输入。
_Avoid_: 边网络、grid graph

**链路（link）**：
单元格流网络上的一条有向单元格对及其轨迹数权重。
_Avoid_: 边、edge

**区域（region）**：
社区检测在单元格流网络上划出、并经确定性后处理的单元格集合及其合并多边形。全部统计量的口径单位。
_Avoid_: 分区、社区、cluster、街区

**片区（district）**：
把区域当节点、区域间通道流当链路再跑一次社区检测得到的区域分组。人工标签只做到这一级。
_Avoid_: 大区、行政区

**区域编码（region code）**：
区域面向人的标识，形如「片区人工标签-区内序号」，区内序号在片区内按格数降序从 1 起编。只用于展示与叙述，统计口径仍是 `region_id`。
_Avoid_: 区域名、区域标签

**冻结划分（frozen partition）**：
在晴天集上产出、被下游全部阶段引用的那一版 `region_cells`。它是运行产物，不进 Git；身份是内容摘要。
_Avoid_: 冻结分区、金划分

**晴天集（clear-day set）**：
四个非雨工作日 `{2020-12-21, 2020-12-22, 2020-12-24, 2020-12-25}`。区域发现只在这四天的合并数据上做一次；12-23 不参与划分。
_Avoid_: 工作日集、研究窗口

## 流量与画像

**出行流（OD flow）**：
来自订单表：行程的解锁点所在区域 → 上锁点所在区域。
_Avoid_: OD 矩阵、需求流

**通道流（channel flow）**：
来自轨迹表：匹配路径依次穿越的区域序列中的相邻转移。
_Avoid_: 路径流、过境流

**轨迹首末流（track OD flow）**：
来自轨迹表：有效轨迹的首区域 → 末区域。只用于与出行流在共享区域对上做代表性对照，不替代出行流。
_Avoid_: 轨迹 OD、轨迹出行流

**一次进入（visit）**：
去抖之后，轨迹在某个区域内的一段连续停留。够格门槛是该段路径 ≥ 100 米，或落在其里程区间内的匹配点 ≥ 2 个。
_Avoid_: 停留、dwell、run

**时段（hour slice）**：
6:00–10:00 内按整点切出的四个一小时区间。轨迹按起始时刻归入一个时段；订单的解锁与上锁事件各按自己发生的时刻归入时段。
_Avoid_: 小时桶、时间片、时间窗

**核心窗口（core window）**：
6:30–9:30。只用起止都落在其内的有效轨迹重算过境率，作为对 6:00/10:00 硬截断偏差的稳健性对照。
_Avoid_: 内窗、安全窗口

**净流入（net inflow）**：
某区域在某时段内的上锁数减解锁数，即该区域自行车存量在该时段的变化；正为汇、负为源。除以分析几何面积得净流入强度。
_Avoid_: 净流、净流量、源汇值

**过境轨迹（transit track）**：
对某区域而言，穿越该区域且首区域与末区域均不为该区域的有效轨迹。首末区域取整条轨迹跨全部路径断点与切断的第一次与最后一次进入。
_Avoid_: 过路轨迹、穿越轨迹、通过轨迹

**过境率（`PI_r`）**：
穿越某区域的轨迹中，过境轨迹所占的比例。
_Avoid_: 穿越率、通过率

**过境弦（transit chord）**：
一条过境轨迹在某区域内从首次进入点到末次离开点的向量；一条轨迹对一个区域只有一条弦。
_Avoid_: 方向向量、进出向量、穿越线

**方向集中度（`R`）**：
过境弦方位的圆统计合成向量长度，取值 0–1；高说明过境方向单一（潮汐）。
_Avoid_: 方向性、各向异性

**轴向集中度（`R_axial`）**：
过境弦方位角加倍后的圆统计合成向量长度，取值 0–1；高说明过境沿同一轴线，不问来回方向。
_Avoid_: 走廊度、轴向性

**功能构成（functional composition）**：
区域分析几何内住宅、就业、教育、交通四类 OSM 面要素的面积份额向量。只用于解释源汇标签，不是标签本身。
_Avoid_: 用地性质、土地利用、POI 构成

**显著流对（significant flow pair）**：
在对应零模型下、经多重比较校正后流量显著高于随机预期的有向区域对。自环不参与判定。
_Avoid_: 强流对、主要流向、重要流

**承载街道（segment usage）**：
承载某区域对流量的物理路段，无方向。仅作可选下钻图层。
_Avoid_: 路段流量、道路负荷

**分析几何 / 成图几何**：
分析几何是只含有流量覆盖的单元格的区域范围，全部统计量以它为准；成图几何是为消除地图孔洞而额外填充过的区域范围，只用于展示。
_Avoid_: 真实边界、显示边界

## 序列与模式

**区域序列（region sequence）**：
一条有效轨迹在一个 piece 内、被无区域段切断之后依次进入的区域列表（连续重复已合并），长度 ≥ 2 才成立。频繁模式挖掘以它为一条记录。
_Avoid_: 轨迹序列、区域链、路径序列

**支持度（support）**：
含某模式的区域序列条数，同一条序列内重复出现只算一次。同一条轨迹因此可能投出多票（实测每轨迹 1.03 条），是硬剔除口径「一票制」的显式例外。
_Avoid_: 频次、出现次数

**连续支持度（contiguous support）**：
某模式作为**连续**子序列出现的区域序列条数，恒不大于支持度。走廊归因只能用它。
_Avoid_: 相邻支持度、路径支持度

## 路网与匹配

**路网（bike network）**：
从 OSM 抽出的、本岛边界 100 米容差内自行车可达道路构成的有向图。
_Avoid_: 道路网、图、OSM 数据

**物理路段（segment）**：
路网中两个相邻交叉口之间的一段道路，无方向，是承载街道的计数单位。
_Avoid_: 路段、way、link

**有向边（directed edge）**：
一条物理路段的一个行驶方向。每条物理路段固定有两条，违反 OSM 单向规则的那条不删除，只标记为不合法方向。
_Avoid_: 边、弧、单向路段

**吸附距离（snap distance）**：
一个定位点到它被匹配上的有向边的垂直距离，是匹配质量的主要量度。
_Avoid_: 匹配误差、偏移、投影距离

**推断段（inferred segment）**：
匹配路径中没有定位点支撑、由最短路补齐的部分。
_Avoid_: 补全段、虚拟路径、插值

**路径断点（path break）**：
两个相邻定位点之间在路网上接不通、匹配路径因此被切开的位置。
_Avoid_: 断链、间断、gap

## 处理与证据

**切分（split）**：
把一个 `BICYCLE_ID` 的点序列断成多条轨迹的动作。判据是时间不递增、间隔过长、速度过高、跳跃过远。
_Avoid_: 分段、断链

**去抖（debounce）**：
把单元格穿越映射到区域并合并连续相同区域之后，把不够门槛的一次进入并入前一次已保留的进入；段首不够格且后面还有进入时丢弃。
_Avoid_: 滤波、平滑、清洗

**无区域段（unassigned gap）**：
单元格不在 `region_cells` 的一段穿越。自身够去抖门槛时切断序列，下一次进入记 `gap_before`；不够门槛则并入前一次进入。
_Avoid_: 空洞、缺口、missing

**硬剔除（hard filter）**：
判定一条轨迹是否可信的通过/不通过规则。剔除的是不可信数据，不是与题目无关的数据。
_Avoid_: 清洗、过滤、质量筛选

**阶段（stage）**：
管线中一个具名的处理步骤，必须报告进入数、保留数与拒绝数。
_Avoid_: 步骤、环节、pass

**运行（run）**：
管线的一次执行，产出该次执行的参数、环境与内容摘要。
_Avoid_: 任务、job、批次

**内容摘要（digest）**：
对一次运行的产出**内容**（而非文件字节表示）计算的摘要，用于判定两次运行是否等价。
_Avoid_: 哈希、校验和、指纹

**基线（baseline）**：
已冻结的期望数字，供后续实现做交叉验证。基线由独立于被验证实现的方法算出，否则不构成验证。
_Avoid_: 期望值、标准答案、金标准

**数据契约（data contract）**：
全部输入文件的哈希与版本记录，可随时复验漂移。
_Avoid_: 清单、manifest、锁文件

**零模型（null model）**：
与观测数据共享指定边际量、但把待检验的那部分结构打散的对照数据。本项目用三个，各有名字：**链路权重零模型**保留单元格流网络的链路集合、只重排权重；**端点重排零模型**随机置换行程的终点区域，行和与列和都精确保持；**支撑内强度零模型**保留观测到的有向区域对集合，按两端区域的观测出入强度之积把总量重分配到这些对上。
_Avoid_: 随机基线、baseline、对照组

**留一日折（leave-one-day fold）**：
晴天集里三天合并作训练侧、剩下一天作验证侧构成的一折，共四折。只用于划分稳定性。
_Avoid_: 交叉验证、折、留一法

**划分相似度（partition similarity）**：
两套划分把同一批元素分组的一致程度。主口径的元素是匹配点、主度量是 AMI；辅口径的元素是单元格，辅度量是元素中心相似度。跨格边长只有点口径成立。
_Avoid_: 一致性、聚类相似度、AMI

**曝光（exposure）**：
某一天进入某项统计的记录条数（有效轨迹数、有效行程数、有进入轨迹数）。跨天比较流量总量前必须先除掉它，否则数据留存差异会被记到出行行为上。
_Avoid_: 样本量、数据量、基数
